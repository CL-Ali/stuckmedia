{
    "name": "stuckmedia",
    "version": "1.0.0",
    "scripts": {
      "vercel-build": "pip install --upgrade pip && pip install --no-cache-dir -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    }
  }